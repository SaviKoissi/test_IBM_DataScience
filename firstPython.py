# Create my first function 
def comp_mean(x):
    #Although there is a function 
    #mean for the purpose of this 
    #class we are creating a 
    #function to do the same task. 
    #x is a vector of numeric 
    ave = sum(x)/len(x)
    print("The mean is", ave)
  
